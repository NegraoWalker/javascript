const names = ["Daniel","Walker","Gabriela","Sandra","Vitória","Adolfo"];

for (personName of names) { //Acessa o valor armazenado em cada índice
    console.log(personName);
}

for (info in names) { //Acessa os índices
    console.log(info);
}
